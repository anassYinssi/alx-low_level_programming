#include "main.h"

/**
 * _islower - Program entry
 * @c: character to be checked
 * Return: 1 for lowercase  and 0 for anything else
 */
int _islower(int c)
{
	if (c >= 97 && i <= 122)
	{
		return (1);
	}
	return (0);
}
