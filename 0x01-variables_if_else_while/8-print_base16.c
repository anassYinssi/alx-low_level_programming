#include <stdlib.h>
#include <stdio.h>
/**
 *  main - print alphabet
 *
 *  Return: always 0 (Success)
 */

int main(void)
{
	int n;
	int m;

	for (n = 48; n <= 57; n++)
	{
		putchar(n);
	}
	for (m = 97; m <= 102; n++)
	{
		putchar(m);
	}

	return (0);
}
